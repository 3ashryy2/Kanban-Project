package com.valeo.kanban.model.enums;

public enum AuditActionType {
    CARD_MOVED,
    GATE_REQUESTED,
    GATE_APPROVED,
    GATE_REJECTED,
    METADATA_UPDATED,
    TASK_ASSIGNED,
    ADMIN_OVERRIDE
}
